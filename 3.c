#include <stdio.h>
main()
{
    int i;
    char arr[10];
    printf("enter the character");
        scanf("%s",&arr);
    printf("the character in the array\n");
        printf("%s",arr);
    return 0;
}
